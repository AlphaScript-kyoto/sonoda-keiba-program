# サーバー引っ越し用スナップショット

このフォルダは、手元PC（当時ユーザー `akimi`）のタスクスケジューラ設定を書き出した記録です。

- XML 内のパスは旧PC固定です。そのままインポートしないでください
- サーバーPCでは `scripts/register_server_tasks.ps1` を使って登録します
- 手順の本体はリポジトリ直下の `SERVER_SETUP_GUIDE.md` です
